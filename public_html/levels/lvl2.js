/* 
 * Level 2
 */

shooter.addEncounter(new PathingEnemy([
    $V([0,100]),
    $V([600, 100]),
    $V([600, 300]),
    $V([0, 300])
], 200, 10, "#917", 1), 1000);

shooter.addEncounter(new PathingEnemy([
    $V([0,100]),
    $V([600, 100]),
    $V([600, 300]),
    $V([0, 300])
], 200, 10, "#917", 1), 1300);

shooter.addEncounter(new PathingEnemy([
    $V([0,100]),
    $V([600, 100]),
    $V([600, 300]),
    $V([0, 300])
], 200, 10, "#917", 1), 1600);

shooter.addEncounter(new PathingEnemy([
    $V([0,100]),
    $V([600, 100]),
    $V([600, 300]),
    $V([0, 300])
], 200, 10, "#917", 1), 1900);

shooter.addEncounter(new PathingEnemy([
    $V([0,100]),
    $V([600, 100]),
    $V([600, 300]),
    $V([0, 300])
], 200, 10, "#917", 1), 2200);
